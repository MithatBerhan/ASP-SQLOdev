@page
@model Verifinalodev.Pages.Personel.EditModel
@{
}
<br>
<h2>Personel Duzenleme</h2>
@if (Model.errorMessage.Length > 0)
{
    <div class="alert alert-danger" role="alert">
        <strong>@Model.errorMessage</strong>
        <button type="button" class="btn-close" data-bs-dismiss='alert' aria-label='Close'></button>
    </div>
}

<form method="POST">
    <input type="hidden" name="id" value="@Model.PerInfo.pid">
    <div class="row mb-3">
        <label class="colcol-sm-3 col-form-label">Name</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="name" value="@Model.PerInfo.pname">
        </div>
    </div>
    <div class="row mb-3">
        <label class="colcol-sm-3 col-form-label">Email</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="email" value="@Model.PerInfo.pemail">
        </div>
    </div>
    <div class="row mb-3">
        <label class="colcol-sm-3 col-form-label">Phone</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="phone" value="@Model.PerInfo.pphone">
        </div>
    </div>
    <div class="row mb-3">
        <label class="colcol-sm-3 col-form-label">Tarih</label>
        <div class="col-sm-6">
            <input type="text" class="form-control" name="adres" value="@Model.PerInfo.ptarih">
        </div>
    </div>

    @if (Model.successMessage.Length > 0)
    {
        <div class="row mb-3">
            <div class="col-sm-9">
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    <strong>@Model.successMessage</strong>
                    <button type="button" class="btn-close" data-bs-dismiss='alert' aria-label='Close'></button>
                </div>
            </div>
        </div>
    }

    <div class="row mb-3">
        <div class="offset-sm-3 col-sm-3 d-grid">
            <button type="submit" class="btn btn-primary">Submit</button>
        </div>
        <div class="col-sm-3 d-grid">
            <a class="btn btn-outline-primary" href="/" role="button">Cancel</a>
        </div>
    </div>
</form>